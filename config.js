// 从浏览器拷贝 Teambition 网址的 cookie 到此变量
const cookie = ""

// 为根地址（项目列表）设置访问密码，字符串为空则不设置
// 其他目录设置访问密码可直接在目录下创建 <pass>.password 文件，例如 1234.password
const root_password = "";

module.exports = {
  cookie,
  root_password,
};
